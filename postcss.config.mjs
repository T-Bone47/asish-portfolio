/** Tailwind CSS v4 — CSS-first config, no tailwind.config.js needed.
 *  Design tokens themselves live in app/globals.css under @theme. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
